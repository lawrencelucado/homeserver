version: '3.9'

services:
  codex:
    image: ghcr.io/datalux-ai/codex:latest
    container_name: codex
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      CODEX_SYSTEM_PROMPT_FILE: /app/prompts/system.md
    volumes:
      - ./codex/prompts:/app/prompts:ro
    restart: unless-stopped
